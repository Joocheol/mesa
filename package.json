{
  "name": "fake-market-workshop",
  "version": "2.0.0",
  "private": true,
  "description": "가짜 주식시장 만들기 — 은행 실무자 대상 5시간 브라우저 워크숍 (속이는 팀과 잡아내는 팀)",
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev_server.mjs 8787",
    "test": "node --test tests/*.test.mjs && python3 -m unittest discover -s tests",
    "build": "python3 scripts/build_sites_worker.py"
  },
  "engines": { "node": ">=22.13" }
}
